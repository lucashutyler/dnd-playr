# Copy to .env and edit. Every value here is the default, so .env is optional.
PORT=3000
HOST=0.0.0.0
# SQLite file. Backing up the app means copying this file.
DB_PATH=data/dnd-playr.db
LOG_LEVEL=info
